node_modules/
power-platform/generative-pages/**/*.compiled.js
dist/
build/
# TypeScript incremental build cache — build artifact, must NOT be committed.
# (Kept on disk locally; deleting the physical file forces a 50–90 min cold rebuild.)
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.vscode/
